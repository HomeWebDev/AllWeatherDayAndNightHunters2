@model List<AllWeatherDayAndNightHunters2.Models.PlayerModel>

@{
    ViewBag.Title = "PlayerView";
}

<h2>Play the game with a existing hunter or create a new hunter</h2>

@using (Html.BeginForm())
{
    <div>
        @Html.DropDownList("playerList")
        <input type="submit" class="btn btn-primary" value="ok"/>
    </div>
    <div>
        @Html.ActionLink("Create new player","PlayerCreate")
    </div>
}